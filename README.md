# Forked

This is a forked repository.
It serves as a temporary fork for drafting and submitting contributions.
As a simplification, the default branch holds this information instead of the upstream main branch which would become outdated and need updating.
